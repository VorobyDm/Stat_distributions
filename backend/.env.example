# Copy to .env (NOT committed) for local dev,
# or set these in the hosting dashboard for prod.

# Comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=http://localhost:5173,https://ungservice.com,https://www.ungservice.com
